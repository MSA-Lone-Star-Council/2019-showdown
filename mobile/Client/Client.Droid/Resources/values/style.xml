<?xml version="1.0" encoding="utf-8" ?>
<resources>
  <!-- inherit from the material theme -->
  <style name="ShowdownOrange" parent="android:Theme.Material">
    <!-- Main theme colors -->
    <!--   your app branding color for the app bar -->
    <item name="android:colorPrimary">@colors/primaryColor</item>
    <!--   darker variant for the status bar and contextual app bars -->
    <item name="android:colorPrimaryDark">@colors/primaryDarkColor</item>
    <!--   theme UI controls like checkboxes and text fields -->
    <item name="android:colorAccent">@colors/secondaryColor</item>
    <!-- -->
    <item name="android:windowBackground">@colors/backgroundColor</item>
    <!-- -->
    <item name="android:navigationBarColor">@colors/navigationBarColor</item>
    <!-- -->
    <item name="android:textColorPrimary">@colors/primaryTextColor</item>
  </style>
</resources>
